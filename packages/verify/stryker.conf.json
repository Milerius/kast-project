{
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "reporters": ["clear-text", "progress"],
  "mutate": ["src/**/*.ts", "!src/**/*.test.ts"],
  "thresholds": { "high": 80, "low": 70, "break": 60 }
}
